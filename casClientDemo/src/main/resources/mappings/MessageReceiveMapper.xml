<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.pms.dataService.dao.MessageReceivedDao">
	<select id="query" parameterType="java.util.Map" resultMap="MessageReceived">
		SELECT a.eperson_id,a.message_id,e.scholar_name FROM message_received AS a LEFT JOIN eperson AS e ON a.eperson_id=e.eperson_id WHERE a.message_id=#{messageId}
	</select>
</mapper>