<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.pms.dataService.dao.CommunityDao">
    <select id="epersonNumOfCommunity" resultType="java.util.Map">
      SELECT community_id as communityId, count(1) as count from community2eperson
         WHERE eperson_id not in (SELECT eperson_id from eperson where visible <![CDATA[<=]]> 0 or isdelete = 1)
         GROUP BY community_id ORDER BY count(1) DESC
   </select>
</mapper>