<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.pms.dataService.dao.ProjectMiddleListDao">
   <select id="query" parameterType="java.util.Map" resultMap="ProjectMiddleList">
	SELECT ptom.middlecheckid,pm.batch,pm.begindate,pm.enddate,pc.project_name,pc.project_code,
pc.charge,pc.datestart,ptom.projectstate,ptom.relaid as relaid,pc.project_id,
ptom.middlecheckdate,ptom.middlecheckinfo,ptom.filejson,ptom.projectmessage
 FROM `project2middlecheck` ptom
JOIN project_column pc ON ptom.projectid=pc.project_id
JOIN projectmiddlecheck pm ON ptom.middlecheckid=pm.middlecheckid
	<trim prefix="WHERE" prefixOverrides="AND|OR">

		<if test="middlecheckid != null">
			and ptom.middlecheckid=#{middlecheckid}
		</if>
		<if test="inputUserId != null">
			and pc.inputUserId=#{inputUserId}
		</if>
		<if test="project_id != null">
			and pc.project_id=#{project_id}
		</if>
		<if test="relaid != null">
			and ptom.relaid=#{relaid}
		</if>
		 <if test="project_name != null and project_name != ''">
            and pc.project_name like concat(concat('%',#{project_name}),'%')
         </if>
          <if test="project_code != null and project_code != ''">
            and pc.project_code like concat(concat('%',#{project_code}),'%')
         </if>
		<if test="project_charge != null and project_charge != ''">
			and pc.charge=#{project_charge}
		</if>
        and pc.state >= 0
	</trim>
</select>

   <select id="queryAllByProjectId" parameterType="java.util.Map" resultMap="ProjectMiddleCheckList">
	SELECT *
 FROM `project2middlecheck` ptom
JOIN project_column pc ON ptom.projectid=pc.project_id
JOIN projectmiddlecheck pm ON ptom.middlecheckid=pm.middlecheckid
	<trim prefix="WHERE" prefixOverrides="AND|OR">
		<if test="project_id != null">
			and pc.project_id=#{project_id}
		</if>
		<if test="relaid != null">
			and ptom.relaid=#{relaid}
		</if>
	</trim>
</select>
</mapper>